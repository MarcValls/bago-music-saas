name: Build APK (disabled — bubblewrap init requires interactive TTY)
on:
  workflow_dispatch:

jobs:
  build-apk:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Build APK requires manual TWA project generation. See META_BUILD_SYSTEMS.md"